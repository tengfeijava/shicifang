
 # bootstrap.yml 文件代替了 application.yml
   #在启动此服务时，此服务会去tensquare_config微服务去加载上传到Git上的base配置文件
 spring:
   cloud:
     config:          # Spring Cloud Config 组件，统一管理配置文件工具
       name: base     # 文件名前段
       profile: dev   #文件中断
       label: master   #指定读取那个分支
       uri: http://127.0.0.1:12000
